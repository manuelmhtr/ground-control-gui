import { defineMessages } from 'react-intl';

export const scope = 'app.components.VideoView';

export default defineMessages({
  emtpyTitle: {
    id: `${scope}.emtpyTitle`,
    defaultMessage: 'Video stream is not available',
  },
});
